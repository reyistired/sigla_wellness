export default function Logo({ onDark = false }) {
  return (
    <a className="brand" href="#top" aria-label="Sigla Wellness, back to top">
      <span className="brand-text">Sigla</span>
    </a>
  );
}
